You are reviewing a small service before making a change. Produce a concise implementation plan with exactly five numbered steps. The service receives JSON jobs, validates an idempotency key, writes a durable checkpoint, performs the job, and records a terminal result. Address correctness under retries, crash recovery, observability, and tests. Do not use tools and do not write code.
